name: mobx_example
description: MobX example project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  flutter_mobx: ^2.0.6+5
  mobx: ^2.1.3
  mobx_codegen: ^2.1.1
  scrollable_positioned_list: ^0.3.5

dev_dependencies:
  build_runner: ^2.3.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true